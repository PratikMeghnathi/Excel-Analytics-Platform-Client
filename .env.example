# =============================================================================
# EXCEL ANALYTICS PLATFORM - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and replace all placeholder values with actual values
# This file configures the React/Vite frontend application
# Variables prefixed with VITE_ are exposed to the browser
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# -----------------------------------------------------------------------------
# Environment mode: development | production
# Controls build optimizations and debugging features
NODE_ENV=development

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
# Backend API base URLs
# Note: VITE_ prefix makes these variables available in browser

# Local development API endpoint
VITE_LOCAL_API_BASE_URL=http://localhost:5000/api

# Production API endpoint
VITE_API_BASE_URL=https://your-backend-domain.com/api

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# ⚠️  VITE_ prefixed variables are PUBLIC and visible in browser
# ⚠️  Never put sensitive data (API keys, secrets) in VITE_ variables
# ⚠️  All VITE_ variables are bundled into the client-side code
# ⚠️  Use backend environment variables for sensitive configuration
# =============================================================================

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder URLs with your actual backend endpoints
# 3. Set NODE_ENV to 'development' for local development
# 4. Set NODE_ENV to 'production' for production builds
# 5. Test API connectivity before deploying
# 6. Ensure backend CORS is configured for your frontend domain
# =============================================================================

# =============================================================================
# ENVIRONMENT-SPECIFIC EXAMPLES
# =============================================================================
# 
# DEVELOPMENT:
# NODE_ENV=development
# VITE_LOCAL_API_BASE_URL=http://localhost:5000/api
#
# PRODUCTION:
# NODE_ENV=production
# VITE_API_BASE_URL=https://api.your-domain.com/api
# =============================================================================